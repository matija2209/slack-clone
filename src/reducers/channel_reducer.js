import {
    SIDEBAR_OPEN
  } from '../actions'

export default function channel_reducer(state,action) {
    const type = action.type
    switch (type) {
        case "value":
            
            break;
    
        default:
            break;
    }
  return (
    state
  )
}
